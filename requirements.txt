# Core runtime. PySpark 3.5+ is required for the array higher-order functions and
# percentile_approx signatures used across ecg/features.py and ecg/eda.py.
pyspark>=3.5,<5.0
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
pyarrow>=12.0
PyYAML>=6.0
scikit-learn>=1.3

# Deep learning (notebooks 06-07). On a machine without a GPU the CPU wheels are
# enough: the network is ~54k parameters and trains in about 25 minutes on 2 cores.
torch>=2.0

# Experiment tracking. Optional at runtime: ecg.training skips MLflow silently
# when it is not installed. Databricks provides it already.
mlflow>=2.9

# Notebooks
jupyterlab>=4.0
ipykernel>=6.25

# Development
pytest>=7.4
